export interface SalesData {
    _id: string,
    totalQuantity: number,
    totalRevenue: number,
    productId: string,
    productName: string
}

export interface DisplayManufacturerSalesProps { } 
